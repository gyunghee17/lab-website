# 연구실 웹사이트 시작하기

코딩을 전혀 몰라도 따라 할 수 있게 썼습니다. 순서대로만 하시면 됩니다.

---

## 0. 지금 상태

`lab-website` 폴더 안에 웹사이트가 이미 완성되어 있습니다.

| 파일 | 무엇인가 |
|---|---|
| `index.html` | Home 페이지 |
| `research.html` | Research 페이지 |
| `professor.html` | Professor 페이지 |
| `members.html` | Members 페이지 |
| `publications.html` | Publications 페이지 |
| `gallery.html` | Gallery 페이지 |
| `contact.html` | Contact 페이지 |
| `style.css` | 디자인(색·글꼴·여백). **7개 페이지가 공유합니다.** |
| `script.js` | 모바일 메뉴 버튼 동작. 건드릴 일 없습니다. |
| `images/` | 사진 넣는 곳 |

**먼저 눈으로 확인하세요.** `index.html`을 더블클릭하면 브라우저에서 바로 열립니다. 인터넷에 올리지 않아도 내 컴퓨터에서 완성된 모습을 볼 수 있습니다.

---

## 1. 내용 채우기

노란색으로 표시된 부분이 채워 넣어야 할 자리입니다. `[대괄호]` 안의 안내문을 실제 내용으로 바꾸면 됩니다.

### 편집 방법

1. **편집기를 하나 설치하세요.** [Visual Studio Code](https://code.visualstudio.com) (무료). 메모장으로도 되지만 VS Code가 훨씬 편합니다.
2. VS Code에서 `lab-website` 폴더를 통째로 엽니다 (File → Open Folder).
3. 왼쪽 파일 목록에서 페이지를 클릭해 엽니다.
4. `<p>`, `<h2>` 같은 꺾쇠 태그는 **건드리지 말고**, 그 사이의 글자만 고칩니다.

```html
<h3>Structural basis of <span class="todo">[process]</span></h3>
        ↓ 이렇게 고칩니다
<h3>Structural basis of ribosome quality control</h3>
```

`<span class="todo"> ... </span>`을 통째로 지우고 그 안에 실제 내용을 쓰면 노란 표시가 사라집니다.

5. 저장(Ctrl+S / Cmd+S) → 브라우저에서 새로고침(F5) → 바로 반영됩니다.

### 가장 먼저 바꿔야 할 것

**`[Lab Name]`** — 7개 파일 모두에 들어 있습니다. VS Code에서 `Ctrl+Shift+H`(Mac은 `Cmd+Shift+H`)를 누르면 폴더 전체 찾아바꾸기가 열립니다. 위칸에 `[Lab Name]`, 아래칸에 실제 이름(예: `Kim`)을 넣고 **Replace All**. 한 번에 다 바뀝니다.

같은 방법으로 바꾸면 좋은 것들:

- `[Full Name]` → 성함 (영문)
- `[id]@hanyang.ac.kr` → 실제 이메일
- `[Building, Room]` → 실제 호실

### 사진 넣기

1. 사진 파일을 `images` 폴더에 넣습니다. 파일 이름은 **영문 소문자와 숫자만** (한글·띄어쓰기 금지).
2. HTML에서 해당 자리의 주석 처리된 줄을 살립니다.

```html
<!-- 이렇게 되어 있는 것을 -->
<div class="pi-photo">
  <!-- Replace with: <img src="images/pi.jpg" alt="[Full Name]"> -->
  Photo placeholder<br>images/pi.jpg
</div>

<!-- 이렇게 바꿉니다 -->
<div class="pi-photo">
  <img src="images/pi.jpg" alt="Prof. Kim">
</div>
```

`images/README.txt`에 어떤 이름의 파일이 어디에 쓰이는지 정리해 두었습니다.

### 항목 추가하기 (구성원, 논문, 사진)

각 페이지에 **"HOW TO ADD..."** 주석이 있습니다. 요령은 모두 같습니다: **기존 블록 하나를 통째로 복사해서 바로 아래에 붙여넣고, 글자만 고친다.**

- 구성원 추가 → `members.html`의 `<div class="person"> ... </div>` 복사
- 논문 추가 → `publications.html`의 `<article class="pub"> ... </article>` 복사
- 사진 추가 → `gallery.html`의 `<figure class="photo"> ... </figure>` 복사

여는 태그와 닫는 태그를 짝으로 함께 복사하는 것만 주의하세요.

### 색 바꾸기

`style.css` 맨 위 `:root { }` 안의 색깔 코드만 바꾸면 사이트 전체 색이 바뀝니다. `--accent`가 링크와 강조 색입니다.

---

## 2. 인터넷에 올리기 (GitHub Pages · 무료)

주소는 `https://아이디.github.io/lab/` 형태가 됩니다. 마우스만으로 가능하고, 명령어는 하나도 필요 없습니다.

### 2-1. GitHub 계정 만들기

[github.com](https://github.com) → Sign up. 아이디가 주소에 그대로 들어가니 신중히 고르세요 (예: `hyu-kimlab`). 이메일 인증까지 마칩니다.

### 2-2. 저장소(repository) 만들기

1. 오른쪽 위 **+** → **New repository**
2. **Repository name**: `lab` (원하는 단어로 해도 됩니다. 주소에 들어갑니다)
3. **Public** 선택 — 반드시 Public이어야 무료로 웹사이트가 됩니다
4. **Create repository**

### 2-3. 파일 올리기

1. 만들어진 저장소 화면에서 **uploading an existing file** 링크 클릭
2. 내 컴퓨터에서 `lab-website` 폴더를 열고, **폴더 안의 파일들을 전부 선택**해서 브라우저 화면에 끌어다 놓습니다
   - ⚠️ `lab-website` 폴더 자체가 아니라 **그 안의 내용물**을 올려야 합니다. `index.html`이 최상위에 있어야 합니다.
   - `images` 폴더는 폴더째로 끌어다 놓으면 됩니다.
3. 아래 **Commit changes** 버튼 클릭

### 2-4. 웹사이트 켜기

1. 저장소 상단 **Settings** 탭
2. 왼쪽 메뉴에서 **Pages**
3. **Source**: `Deploy from a branch`
4. **Branch**: `main` / 폴더는 `/ (root)` → **Save**
5. 1~2분 기다린 뒤 그 페이지를 새로고침하면 맨 위에 주소가 뜹니다

끝입니다. 이제 그 주소를 명함이나 논문에 적으시면 됩니다.

### 2-5. 이후 내용 수정

방법 A (간단): GitHub 저장소에서 파일 이름 클릭 → 연필 아이콘 ✏️ → 브라우저에서 바로 고치고 **Commit changes**. 1분 뒤 사이트에 반영됩니다.

방법 B: 내 컴퓨터에서 고친 뒤 **Add file → Upload files**로 같은 이름의 파일을 다시 올리면 덮어쓰기 됩니다.

---

## 3. 자주 겪는 문제

| 증상 | 원인 / 해결 |
|---|---|
| 사이트가 흰 화면, 글꼴도 이상함 | `style.css`가 `index.html`과 **같은 폴더**에 있어야 합니다. 폴더 구조를 바꾸지 마세요. |
| 사진이 안 보이고 깨진 아이콘 | 파일 이름 대소문자까지 정확히 일치해야 합니다. `Pi.JPG` ≠ `pi.jpg` |
| 수정했는데 안 바뀜 | 브라우저 강력 새로고침 (Ctrl+Shift+R / Cmd+Shift+R) |
| 404 페이지가 뜸 | `index.html`이 저장소 최상위에 있는지 확인. 폴더 안에 들어가 있으면 안 됩니다. |
| 레이아웃이 무너짐 | 태그를 짝으로 지우지 않은 경우가 대부분입니다. 되돌리기(Ctrl+Z) 후 다시 시도하세요. |

---

## 4. 나중에 하면 좋은 것

- **자체 도메인 연결**: 도메인을 사면 GitHub Pages Settings에서 연결할 수 있습니다. 학교에서 `hanyang.ac.kr` 하위 주소를 줄 수도 있으니 학과에 문의해 보세요.
- **Google Scholar 자동 연동**: 논문 목록을 매번 손으로 넣기 번거로우면 자동으로 불러오게 만들 수 있습니다.
- **한국어 페이지 추가**: 학부생 모집용으로 한국어 버전을 따로 두는 것도 좋습니다.

이 중 필요한 게 생기면 언제든 말씀해 주세요. 내용을 불러주시면 제가 대신 채워 넣어 드리는 것도 가능합니다.
